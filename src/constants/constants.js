export const messageTypes = {
  register: 'register',
  call: 'call',
};

export const responseType = {
  ok: 'ok',
  failed: 'failed',
};

export const ROUTE = {
  REGISTER: '/',
  CALL: '/call',
  HISTORY: '/history',
};
